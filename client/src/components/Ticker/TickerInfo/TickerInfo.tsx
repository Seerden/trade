/**
 * Display
 * - ticker name
 * - last close price (also open price?)
 * - last day % change
 *  - interact to change to last week, month, etc.
 * - industry
 * - market cap
 * - float
 * - link to filings
 * - link to news, finviz, ...anything else?
 *
 *
 */

import { StyledTickerInfo } from "./TickerInfo.style";

const TickerInfo = (props) => {
    return <StyledTickerInfo>{/*  */}</StyledTickerInfo>;
};

export default TickerInfo;
